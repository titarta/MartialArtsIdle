/**
 * spiritGarden.js — data + pure logic for the Spirit Garden producer minigame.
 *
 * The garden is a SELF-CONTAINED cropping + alchemy loop:
 *   sow spirit seeds → crops ripen over real time (offline-safe) → harvest into
 *   a basket → spend the basket three ways:
 *     Sell   → Spirit Dew (the garden-local soft currency)
 *     Brew   → a timed qi/s elixir buff (feeds the main cultivation economy)
 *     Channel→ an instant QI burst (the shared minigame cash-out model)
 *
 * Spirit Dew (灵露) never leaves the garden; QI only enters via the optional
 * Channel cash-out. So the loop stays fully playable even if the player never
 * unlocks another minigame and never sits on a dead resource.
 *
 * Persistence: one localStorage key (mai_garden). The React component holds
 * only transient UI state. Growth is derived from plantedAt timestamps, so
 * offline progress needs no background ticking: when the player returns, the
 * crops are simply already ripe.
 *
 * STATUS: v1 STARTING VALUES, not balanced final content. Tune SEED / RECIPE
 * costs + durations against playtest and scripts/sim-cultivation.mjs.
 */
import { RARITY } from './materials';

const KEY = 'mai_garden';
// VERSION 2 (2026-06-08): garden plants graduated from procedural same-shape
// herb sprites (HERBS dict in materials.js) to their own dedicated plant
// catalogue with bespoke art and 4 growth-stage sprites per plant. The
// migration remaps old herb ids (iron_herb_1 etc.) to the new garden plant
// ids (spirit_mint etc.) so existing saves don't lose their plots.
const VERSION = 2;

// ── Tunable economy constants ────────────────────────────────────────────────
export const MAX_PLOTS   = 6;     // hard ceiling on garden beds
export const START_PLOTS = 4;     // beds a fresh garden opens with
export const STARTER_DEW = 20;    // grant on first open, seeds the bootstrap
export const DISCOVERY_BONUS = 5; // Dew awarded the first time a herb is harvested

// Channel → QI cash-out shaping. A basket worth CHANNEL_TARGET (in Dew value)
// maps to a max-performance burst; below CHANNEL_MIN_VALUE the option is locked
// so a single cheap crop cannot farm the shared reward-band floor.
export const CHANNEL_MIN_VALUE = 40;
export const CHANNEL_TARGET    = 200;

// Dew price to unlock the Nth plot. Absent key = not purchasable (maxed).
export const PLOT_COSTS = { 5: 150, 6: 400 };

const MIN = 60_000;
const HR  = 60 * MIN;

// ── Seeds ─────────────────────────────────────────────────────────────────────
// Garden plants are a SELF-CONTAINED taxonomy (separate from HERBS in
// materials.js, which is the world-drop herb table). Each plant has its own
// name, rarity tier, palette accent, and 128x128 sprite sheet at
// /sprites/plants/{id}.png — laid out as a 2x2 grid of 64x64 frames
// (seed / sprout / growing / ripe quadrants). Generated by scripts/gen_plants.py.
//
// dewCost is paid at plant time (no separate seed inventory). The tier-1
// Spirit Mint is free and infinite — the permanent anti-stuck floor of
// the loop.
const RAW_SEEDS = [
  { id: 'spirit_mint',         dewCost: 0,  growMs: 3 * MIN,   sell: 2,  yield: 1, free: true,
    name: 'Spirit Mint',         rarity: 'Iron',    color: '#9ca3af' },
  { id: 'cinnabar_bloom',      dewCost: 4,  growMs: 8 * MIN,   sell: 3,  yield: 1,
    name: 'Cinnabar Bloom',      rarity: 'Iron',    color: '#d04a30' },
  { id: 'jade_lotus_bud',      dewCost: 12, growMs: 30 * MIN,  sell: 8,  yield: 1,
    name: 'Jade Lotus Bud',      rarity: 'Bronze',  color: '#cd7f32' },
  { id: 'moonleaf_vine',       dewCost: 20, growMs: 1 * HR,    sell: 12, yield: 1,
    name: 'Moonleaf Vine',       rarity: 'Bronze',  color: '#cd7f32' },
  { id: 'dragonscale_ginseng', dewCost: 50, growMs: 3 * HR,    sell: 30, yield: 2,
    name: 'Dragonscale Ginseng', rarity: 'Silver',  color: '#c0c2ca' },
  { id: 'phoenix_tail_grass',  dewCost: 90, growMs: 8 * HR,    sell: 55, yield: 2,
    name: 'Phoenix Tail Grass',  rarity: 'Silver',  color: '#c0c2ca' },
];

/** Enriched, display-ready seed list (sprite path folded in). */
export const SEEDS = RAW_SEEDS.map((s) => ({
  ...s,
  // Bronze accent default if a plant's rarity isn't in the RARITY table.
  color:  s.color || RARITY[s.rarity]?.color || '#9ca3af',
  sprite: `/sprites/plants/${s.id}.png`,
}));

export const SEEDS_BY_ID = Object.fromEntries(SEEDS.map((s) => [s.id, s]));

/** Plants teased in the almanac but not yet cultivable (v2 content). The
 *  sprite sheets exist at /sprites/plants/{id}.png — same 2x2 stage layout
 *  as the unlocked plants. Each LOCKED row has its display name + rarity
 *  here so the almanac can render the chip without faking a missing entry. */
export const LOCKED_SEEDS_META = [
  { id: 'gold_crown_peony',     name: 'Gold Crown Peony',     rarity: 'Gold',         color: '#f5c842' },
  { id: 'black_iron_reed',      name: 'Black Iron Reed',      rarity: 'Gold',         color: '#f5c842' },
  { id: 'heaven_pierce_bamboo', name: 'Heaven Pierce Bamboo', rarity: 'Transcendent', color: '#c084fc' },
  { id: 'soul_lily',            name: 'Soul Lily',            rarity: 'Transcendent', color: '#c084fc' },
];

/** Backwards-compat: id-only array for callers that just need the ids. */
export const LOCKED_SEEDS = LOCKED_SEEDS_META.map((s) => s.id);

export const LOCKED_SEEDS_BY_ID = Object.fromEntries(
  LOCKED_SEEDS_META.map((s) => [s.id, { ...s, sprite: `/sprites/plants/${s.id}.png` }])
);

/** How many distinct plants are discoverable right now (almanac denominator). */
export const ALMANAC_TOTAL = SEEDS.length;

// ── Save migration: old herb ids → new plant ids ────────────────────────────
// VERSION 1 saves had plot.seed = iron_herb_1 / bronze_herb_2 / etc. VERSION 2
// remaps these to the new garden plant ids so the player's growing plots aren't
// silently nulled out by the SEEDS_BY_ID lookup. Same goes for basket entries.
const PLANT_ID_MIGRATION = {
  iron_herb_1:         'spirit_mint',
  iron_herb_2:         'cinnabar_bloom',
  bronze_herb_1:       'jade_lotus_bud',
  bronze_herb_2:       'moonleaf_vine',
  silver_herb_1:       'dragonscale_ginseng',
  silver_herb_2:       'phoenix_tail_grass',
  gold_herb_1:         'gold_crown_peony',
  gold_herb_2:         'black_iron_reed',
  transcendent_herb_1: 'heaven_pierce_bamboo',
  transcendent_herb_2: 'soul_lily',
};
const migratePlantId = (id) => PLANT_ID_MIGRATION[id] || id;

// ── Recipes (elixirs) ──────────────────────────────────────────────────────────
// One active buff slot. Brewing replaces any current buff. Each elixir mirrors a
// flat qi/s multiplier into the live cultivation rate for its duration.
// Inputs reference the new garden plant ids. Elixir names + lore are themed
// around the dominant plant in each recipe.
const RAW_RECIPES = [
  // Verdant Tonic — pure Spirit Mint, the early-game heal-up. Uses the free
  // plant entirely so a brand-new player can brew their first elixir without
  // ever spending Dew on seeds.
  { id: 'e_verdant', name: 'Verdant Tonic',       inputs: { spirit_mint: 3 },
    mult: 1.12, durationMs: 30 * MIN },
  // Jade Dew Draught — Cinnabar Bloom's inner fire tempered by Jade Lotus Bud's
  // calm. Mid-tier balance brew.
  { id: 'e_jade',    name: 'Jade Dew Draught',    inputs: { cinnabar_bloom: 2, jade_lotus_bud: 1 },
    mult: 1.25, durationMs: 45 * MIN },
  // Spirit Bloom Elixir — high tier, Moonleaf yin energy concentrated by
  // Dragonscale Ginseng's coveted root.
  { id: 'e_spirit',  name: 'Spirit Bloom Elixir', inputs: { moonleaf_vine: 2, dragonscale_ginseng: 1 },
    mult: 1.45, durationMs: 1 * HR },
];

export const RECIPES = RAW_RECIPES.map((r) => ({
  ...r,
  pct:  Math.round((r.mult - 1) * 100),
  desc: `+${Math.round((r.mult - 1) * 100)}% qi/s for ${growLabel(r.durationMs)}.`,
}));

export const RECIPES_BY_ID = Object.fromEntries(RECIPES.map((r) => [r.id, r]));

// ── Persistence ─────────────────────────────────────────────────────────────────
export function defaultGarden() {
  return {
    v: VERSION,
    dew: STARTER_DEW,
    plotCount: START_PLOTS,
    plots: Array.from({ length: START_PLOTS }, () => null), // each: null | { seed, at }
    basket: {},      // herbId → count
    discovered: {},  // herbId → true (almanac)
    buff: null,      // null | { recipeId, mult, expiresAt }
    stats: { planted: 0, harvested: 0, brewed: 0, channeled: 0 },
  };
}

function migrate(data) {
  const base = defaultGarden();
  const g = { ...base, ...data };
  g.basket     = { ...(data.basket || {}) };
  g.discovered = { ...(data.discovered || {}) };
  g.stats      = { ...base.stats, ...(data.stats || {}) };
  g.dew        = Number.isFinite(data.dew) ? Math.max(0, data.dew) : STARTER_DEW;
  g.plotCount  = Math.max(START_PLOTS, Math.min(MAX_PLOTS, data.plotCount || START_PLOTS));

  // VERSION 2 (2026-06-08): remap old herb ids to new garden plant ids in
  // both the basket and any growing plots, so saves created before the
  // catalogue refactor don't lose their progress. Same idea as a database
  // migration — only the storage representation changes, gameplay state is
  // preserved as faithfully as possible.
  if ((data.v || 1) < 2) {
    const remappedBasket = {};
    for (const [oldId, n] of Object.entries(g.basket)) {
      const newId = migratePlantId(oldId);
      remappedBasket[newId] = (remappedBasket[newId] || 0) + n;
    }
    g.basket = remappedBasket;

    const remappedDiscovered = {};
    for (const id of Object.keys(g.discovered)) {
      remappedDiscovered[migratePlantId(id)] = true;
    }
    g.discovered = remappedDiscovered;
  }

  // Reconcile the plot array length against plotCount. After id migration,
  // any plot whose seed still doesn't resolve gets nulled (e.g. a seed id
  // that was removed entirely).
  const raw = Array.isArray(data.plots) ? data.plots.slice(0, g.plotCount) : [];
  while (raw.length < g.plotCount) raw.push(null);
  g.plots = raw.map((p) => {
    if (!p || !Number.isFinite(p.at)) return null;
    const migratedId = migratePlantId(p.seed);
    if (!SEEDS_BY_ID[migratedId]) return null;
    return {
      seed: migratedId,
      at: p.at,
      ...(Number.isFinite(p.growMs) ? { growMs: p.growMs } : {}),
    };
  });

  if (g.buff && !(g.buff.expiresAt > 0)) g.buff = null;
  g.v = VERSION;
  return g;
}

export function loadGarden() {
  try {
    const raw = localStorage.getItem(KEY);
    if (!raw) return defaultGarden();
    return migrate(JSON.parse(raw));
  } catch {
    return defaultGarden();
  }
}

export function saveGarden(g) {
  try {
    localStorage.setItem(KEY, JSON.stringify(g));
  } catch {
    /* storage full / unavailable — non-fatal for an idle minigame */
  }
}

// ── Growth derivation (offline-safe, time-based) ────────────────────────────────
// Four growth stages mapped to the four 64x64 quadrants of each plant's sprite
// sheet (seed / sprout / growing / ripe = bloom). Thresholds split the grow
// time into roughly equal thirds so each non-bloom stage gets visible airtime.
//   age <  33% grow → 'seed'      (top-left  quadrant)
//   age <  66% grow → 'sprout'    (top-right quadrant)
//   age < 100% grow → 'growing'   (bottom-left quadrant)
//   age >= 100%     → 'bloom'     (bottom-right quadrant — the harvestable one)
export function stageOf(plot, now = Date.now()) {
  if (!plot) return 'empty';
  const seed = SEEDS_BY_ID[plot.seed];
  if (!seed) return 'empty';
  const grow = Number.isFinite(plot.growMs) ? plot.growMs : seed.growMs;
  const age = now - plot.at;
  if (age >= grow)         return 'bloom';
  if (age >= grow * 0.66)  return 'growing';
  if (age >= grow * 0.33)  return 'sprout';
  return 'seed';
}

/** 0..1 growth fraction for a progress ring. */
export function growthProgress(plot, now = Date.now()) {
  if (!plot) return 0;
  const seed = SEEDS_BY_ID[plot.seed];
  if (!seed) return 0;
  const grow = Number.isFinite(plot.growMs) ? plot.growMs : seed.growMs;
  return Math.max(0, Math.min(1, (now - plot.at) / grow));
}

/** Count of ripe (harvestable) plots. */
export function ripeCount(g, now = Date.now()) {
  return g.plots.reduce((n, p) => n + (stageOf(p, now) === 'bloom' ? 1 : 0), 0);
}

/** Timestamp the next plot will ripen, or null if nothing is growing. */
export function nextRipeAt(g, now = Date.now()) {
  let soonest = Infinity;
  for (const p of g.plots) {
    if (!p) continue;
    const seed = SEEDS_BY_ID[p.seed];
    if (!seed) continue;
    const at = p.at + (Number.isFinite(p.growMs) ? p.growMs : seed.growMs);
    if (at > now && at < soonest) soonest = at;
  }
  return soonest === Infinity ? null : soonest;
}

// ── Plot actions (pure: return a fresh garden, never mutate) ─────────────────────
export function plantSeed(g, plotIndex, seedId, now = Date.now(), growMult = 1) {
  const seed = SEEDS_BY_ID[seedId];
  if (!seed) return { ok: false, reason: 'no-seed', garden: g };
  if (plotIndex < 0 || plotIndex >= g.plotCount) return { ok: false, reason: 'bad-plot', garden: g };
  if (g.plots[plotIndex]) return { ok: false, reason: 'occupied', garden: g };
  const cost = seed.dewCost || 0;
  if (cost > g.dew) return { ok: false, reason: 'dew', garden: g };

  const plots = g.plots.slice();
  // Eternal Tree 'Fertile Soil' (soil) bakes a faster grow time into the plot
  // at sow time (growMult < 1), so the speed-up survives reloads via growMs.
  plots[plotIndex] = { seed: seedId, at: now, growMs: Math.max(1, Math.round(seed.growMs * (growMult > 0 ? growMult : 1))) };
  return {
    ok: true,
    garden: { ...g, dew: g.dew - cost, plots, stats: { ...g.stats, planted: g.stats.planted + 1 } },
  };
}

export function harvestPlot(g, plotIndex, now = Date.now()) {
  const plot = g.plots[plotIndex];
  if (stageOf(plot, now) !== 'bloom') return { ok: false, reason: 'not-ripe', garden: g };

  const seed   = SEEDS_BY_ID[plot.seed];
  const herbId = plot.seed;
  const amount = seed.yield || 1;

  const basket = { ...g.basket, [herbId]: (g.basket[herbId] || 0) + amount };
  const plots  = g.plots.slice();
  plots[plotIndex] = null;

  const firstTime  = !g.discovered[herbId];
  const discovered = { ...g.discovered, [herbId]: true };
  const dew        = g.dew + (firstTime ? DISCOVERY_BONUS : 0);

  return {
    ok: true,
    firstTime,
    gained: { herbId, amount },
    garden: { ...g, basket, plots, discovered, dew, stats: { ...g.stats, harvested: g.stats.harvested + amount } },
  };
}

/** Harvest every ripe plot in one pass. Returns aggregate + newly discovered ids. */
export function harvestAll(g, now = Date.now()) {
  let cur = g;
  let total = 0;
  const firsts = [];
  for (let i = 0; i < cur.plots.length; i++) {
    if (stageOf(cur.plots[i], now) === 'bloom') {
      const r = harvestPlot(cur, i, now);
      if (r.ok) {
        cur = r.garden;
        total += r.gained.amount;
        if (r.firstTime) firsts.push(r.gained.herbId);
      }
    }
  }
  return { garden: cur, total, firsts };
}

// ── Basket valuation ─────────────────────────────────────────────────────────────
export function basketValue(g) {
  let v = 0;
  for (const [id, n] of Object.entries(g.basket)) {
    const seed = SEEDS_BY_ID[id];
    if (seed) v += (seed.sell || 0) * n;
  }
  return v;
}

export function basketCount(g) {
  return Object.values(g.basket).reduce((a, b) => a + b, 0);
}

// ── Sell → Dew ────────────────────────────────────────────────────────────────
export function sellHerb(g, herbId) {
  const n = g.basket[herbId] || 0;
  const seed = SEEDS_BY_ID[herbId];
  if (n <= 0 || !seed) return { ok: false, gained: 0, garden: g };
  const gained = (seed.sell || 0) * n;
  const basket = { ...g.basket };
  delete basket[herbId];
  return { ok: true, gained, garden: { ...g, dew: g.dew + gained, basket } };
}

export function sellBasket(g) {
  const gained = basketValue(g);
  if (gained <= 0) return { ok: false, gained: 0, garden: g };
  return { ok: true, gained, garden: { ...g, dew: g.dew + gained, basket: {} } };
}

// ── Brew → timed qi/s buff ──────────────────────────────────────────────────────
export function canBrew(g, recipeId) {
  const r = RECIPES_BY_ID[recipeId];
  if (!r) return false;
  return Object.entries(r.inputs).every(([id, n]) => (g.basket[id] || 0) >= n);
}

export function brew(g, recipeId, now = Date.now(), durationMult = 1) {
  const r = RECIPES_BY_ID[recipeId];
  if (!r) return { ok: false, reason: 'no-recipe', garden: g };
  if (!canBrew(g, recipeId)) return { ok: false, reason: 'inputs', garden: g };

  const basket = { ...g.basket };
  for (const [id, n] of Object.entries(r.inputs)) {
    basket[id] = (basket[id] || 0) - n;
    if (basket[id] <= 0) delete basket[id];
  }
  // Eternal Tree 'Lingering Brew' (linger) extends the buff duration (mult > 1).
  const buff = { recipeId, mult: r.mult, expiresAt: now + r.durationMs * (durationMult > 0 ? durationMult : 1) };
  return { ok: true, garden: { ...g, basket, buff, stats: { ...g.stats, brewed: g.stats.brewed + 1 } } };
}

/** The live qi/s multiplier App.jsx mirrors into cultivation. 1 = no buff. */
export function gardenActiveQiMult(g, now = Date.now()) {
  if (!g || !g.buff) return 1;
  return g.buff.expiresAt > now ? (g.buff.mult || 1) : 1;
}

export function buffRemainingMs(g, now = Date.now()) {
  if (!g || !g.buff) return 0;
  return Math.max(0, g.buff.expiresAt - now);
}

// ── Channel → QI burst ──────────────────────────────────────────────────────────
/** 0..1 performance for computeReward, driven by basket Dew-value. */
export function channelPerformance(g) {
  return Math.min(1, basketValue(g) / CHANNEL_TARGET);
}

export function canChannel(g) {
  return basketValue(g) >= CHANNEL_MIN_VALUE;
}

/** Empty the basket after a successful Channel cash-out. */
export function clearBasket(g) {
  return { ...g, basket: {}, stats: { ...g.stats, channeled: g.stats.channeled + 1 } };
}

// ── Plot expansion (Dew sink) ────────────────────────────────────────────────────
export function nextPlotCost(plotCount) {
  return PLOT_COSTS[plotCount + 1] ?? null;
}

export function expandPlot(g) {
  const cost = nextPlotCost(g.plotCount);
  if (cost == null) return { ok: false, reason: 'maxed', garden: g };
  if (g.dew < cost) return { ok: false, reason: 'dew', garden: g };
  const plots = g.plots.slice();
  plots.push(null);
  return { ok: true, garden: { ...g, dew: g.dew - cost, plotCount: g.plotCount + 1, plots } };
}

// ── Almanac ─────────────────────────────────────────────────────────────────────
export function discoveredCount(g) {
  return Object.keys(g.discovered || {}).filter((id) => g.discovered[id]).length;
}

// ── Small format helper (durations shown on seeds / recipes) ─────────────────────
// Pass i18next's `t` so the suffix ("min" / "hr") localises. Falls back to the
// English unit when `t` isn't provided (e.g. data exports for tests/sims).
export function growLabel(ms, t) {
  const min = ms / 60000;
  if (min < 60) {
    const n = Math.round(min);
    return t ? t('common.minSuffix', { n }) : `${n} min`;
  }
  const hr = min / 60;
  const n = hr % 1 === 0 ? String(hr) : hr.toFixed(1);
  return t ? t('common.hrSuffix', { n }) : `${n} hr`;
}

// ── Codex queries ───────────────────────────────────────────────────────────
// Surfaced through the global Codex modal under the 'Garden' tab. Two
// sections: plants discovered (the existing almanac, repackaged) and
// elixir recipes (always visible since the brewhouse exposes them).

const PLANT_HINTS = {
  spirit_mint:         'Sow the free starter seed and harvest your first plot.',
  cinnabar_bloom:      'Bronze-tier seed. Buy with Spirit Dew, sow, harvest.',
  jade_lotus_bud:      'Bronze-tier seed. Sow and harvest to discover.',
  moonleaf_vine:       'Silver-tier seed. Sow and harvest to discover.',
  dragonscale_ginseng: 'Silver-tier seed. Sow and harvest to discover.',
  phoenix_tail_grass:  'Silver-tier seed. Sow and harvest to discover.',
  gold_crown_peony:    'Gold-tier seed. Requires a future content unlock.',
  black_iron_reed:     'Gold-tier seed. Requires a future content unlock.',
  heaven_pierce_bamboo:'Transcendent-tier seed. Requires a future content unlock.',
  soul_lily:           'Transcendent-tier seed. Requires a future content unlock.',
};

// `t` (i18next translator) is optional; when present, tier line, the
// "future content" hint, section labels, and recipe descs localise.
export function getGardenCodexEntries(g, t) {
  const disc = g.discovered || {};
  const plantEntries = SEEDS.map((s) => {
    const discovered = !!disc[s.id];
    const tierLine = t
      ? t('garden.codexTierLine', { rarity: s.rarity, time: growLabel(s.growMs, t) })
      : `Tier: ${s.rarity}. Grows in ${growLabel(s.growMs)}.`;
    return {
      id:   s.id,
      name: discovered ? s.name : '???',
      desc: discovered ? tierLine : null,
      hint: discovered ? null : (PLANT_HINTS[s.id] || ''),
      discovered,
      color: s.color,
      sprite: s.sprite,
    };
  });
  const lockedHint = t ? t('garden.codexHintLocked') : 'Locked plant. Future content.';
  for (const id of LOCKED_SEEDS) {
    const meta = LOCKED_SEEDS_BY_ID[id];
    plantEntries.push({
      id,
      name: '???',
      desc: null,
      hint: PLANT_HINTS[id] || lockedHint,
      discovered: false,
      color: meta?.color,
      sprite: `/sprites/plants/${id}.png`,
    });
  }
  const recipeEntries = RECIPES.map((r) => ({
    id:         r.id,
    name:       r.name,
    desc:       t
      ? t('garden.recipeDescTemplate', { pct: r.pct, time: growLabel(r.durationMs, t) })
      : r.desc,
    hint:       null,
    discovered: true,
  }));
  const plantsLabel  = t ? t('garden.codexPlantsLabel')  : 'Plants';
  const recipesLabel = t ? t('garden.codexRecipesLabel') : 'Recipes';
  return [
    { id: 'plants',  label: plantsLabel,  entries: plantEntries },
    { id: 'recipes', label: recipesLabel, entries: recipeEntries },
  ];
}

export function getGardenCodexProgress(g) {
  const disc = g.discovered || {};
  const plantsT = SEEDS.length + LOCKED_SEEDS.length;
  const plantsD = SEEDS.filter(s => disc[s.id]).length;
  const recipesT = RECIPES.length;
  const recipesD = RECIPES.length; // recipes always visible
  return {
    total:      plantsT + recipesT,
    discovered: plantsD + recipesD,
  };
}
